[package]
name = "amigo-nanoid"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
nanoid = "0.4"

[build-dependencies]
napi-build = "2"
