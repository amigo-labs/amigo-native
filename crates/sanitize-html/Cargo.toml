[package]
name = "amigo-sanitize-html"
version = "0.3.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-sanitize-html-core = { path = "../_sanitize-html-core" }

[build-dependencies]
napi-build = "2"
