[package]
name = "amigo-fuse"
version = "0.3.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-fuse-core = { path = "../_fuse-core" }

[build-dependencies]
napi-build = "2"
