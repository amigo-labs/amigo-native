[package]
name = "amigo-tldts"
version = "0.3.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-tldts-core = { path = "../_tldts-core" }

[build-dependencies]
napi-build = "2"
