[package]
name = "amigo-slugify"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
unicode-normalization = "0.1"
deunicode = "1"

[build-dependencies]
napi-build = "2"
