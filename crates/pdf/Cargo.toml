[package]
name = "amigo-pdf"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
printpdf = { version = "0.7", default-features = false }

[build-dependencies]
napi-build = "2"
