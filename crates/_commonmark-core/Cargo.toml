[package]
name = "amigo-commonmark-core"
version = "0.1.1"
edition = "2024"
publish = false

[lib]

[dependencies]
pulldown-cmark = { version = "0.13", default-features = false, features = ["html"] }
