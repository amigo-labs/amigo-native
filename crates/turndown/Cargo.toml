[package]
name = "amigo-turndown"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-turndown-core = { path = "../_turndown-core" }

[build-dependencies]
napi-build = "2"
