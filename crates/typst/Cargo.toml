[package]
name = "amigo-typst"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-typst-core = { path = "../_typst-core" }

[build-dependencies]
napi-build = "2"
