[package]
name = "amigo-svgo"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-svgo-core = { path = "../_svgo-core" }

[build-dependencies]
napi-build = "2"
