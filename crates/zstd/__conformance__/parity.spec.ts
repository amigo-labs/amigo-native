import { describe, it, expect } from 'vitest'
// import { something } from '../index.js'

describe('zstd — parity invariants', () => {
  it.todo('handcrafted input → expected output assertions')
  it.todo('safe-default assertions (dropped unsafe input stays dropped)')
})
