[package]
name = "amigo-zstd"
version = "0.3.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-zstd-core = { path = "../_zstd-core" }

[build-dependencies]
napi-build = "2"
