{
  "name": "@amigo-labs/_ffi-bench",
  "version": "0.0.0",
  "private": true,
  "description": "Internal: measures NAPI call overhead for docs/BASELINE.md",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "ffi-bench",
    "targets": [
      "x86_64-unknown-linux-gnu"
    ]
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "bench": "vitest bench"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.2",
    "vitest": "^3.0.0"
  }
}
