[package]
name = "amigo-pixelmatch"
version = "0.3.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-pixelmatch-core = { path = "../_pixelmatch-core" }

[build-dependencies]
napi-build = "2"
