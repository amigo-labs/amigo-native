[package]
name = "amigo-diff"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-diff-core = { path = "../_diff-core" }

[build-dependencies]
napi-build = "2"
