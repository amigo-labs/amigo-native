[package]
name = "amigo-diff"
version = "0.1.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
similar = { version = "2.6", features = ["text", "inline", "unicode"] }

[build-dependencies]
napi-build = "2"
