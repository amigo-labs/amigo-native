[package]
name = "amigo-deepmerge"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-deepmerge-core = { path = "../_deepmerge-core" }
serde_json = "1"

[build-dependencies]
napi-build = "2"
