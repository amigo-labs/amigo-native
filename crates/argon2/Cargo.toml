[package]
name = "amigo-argon2"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
argon2 = { version = "0.5", features = ["std"] }

[build-dependencies]
napi-build = "2"
