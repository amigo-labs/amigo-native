[package]
name = "amigo-zip"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { workspace = true }
napi-derive = { workspace = true }
amigo-zip-core = { path = "../_zip-core" }

[build-dependencies]
napi-build = "2"
